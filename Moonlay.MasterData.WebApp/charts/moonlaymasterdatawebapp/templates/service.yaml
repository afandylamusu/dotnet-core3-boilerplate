apiVersion: v1
kind: Service
metadata:
  name: {{ template "moonlaymasterdatawebapp.fullname" . }}
  labels:
    app: {{ template "moonlaymasterdatawebapp.name" . }}
    chart: {{ template "moonlaymasterdatawebapp.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "moonlaymasterdatawebapp.name" . }}
    release: {{ .Release.Name }}
