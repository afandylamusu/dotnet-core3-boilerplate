apiVersion: v1
kind: Service
metadata:
  name: {{ include "mdm-chart.fullname" . }}
  labels:
    {{- include "mdm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mdm-chart.selectorLabels" . | nindent 4 }}
