apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mdm-chart.fullname" . }}-test-connection"
  labels:
{{ include "mdm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mdm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
