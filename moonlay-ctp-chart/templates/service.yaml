apiVersion: v1
kind: Service
metadata:
  name: {{ include "moonlay-ctp-chart.fullname" . }}
  labels:
    {{- include "moonlay-ctp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moonlay-ctp-chart.selectorLabels" . | nindent 4 }}
