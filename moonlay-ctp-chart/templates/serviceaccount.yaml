{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "moonlay-ctp-chart.serviceAccountName" . }}
  labels:
{{ include "moonlay-ctp-chart.labels" . | nindent 4 }}
{{- end -}}
