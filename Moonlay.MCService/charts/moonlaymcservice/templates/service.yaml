apiVersion: v1
kind: Service
metadata:
  name: {{ template "moonlaymcservice.fullname" . }}
  labels:
    app: {{ template "moonlaymcservice.name" . }}
    chart: {{ template "moonlaymcservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "moonlaymcservice.name" . }}
    release: {{ .Release.Name }}
